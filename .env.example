DATABASE_URL=sqlite:///./data/ledger.db
# true seeds the demo history; set to false to run without demo data.
DEMO_MODE=true
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Required for automatic backups and for encrypted attachment storage. Keep this outside the DB and outside git.
BACKUP_ENCRYPTION_KEY=
